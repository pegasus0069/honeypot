# Honeypot
Advanced intrusion detection and alerting systems, defining precise triggers for suspicious activities based on network protocols such as ICMP (for pings) and SSH (Secure Shell) connection attempts, ensuring timely threat notification for server clusters and local networks. (Utilized Node.js, React.js, REST APIs, Docker)
